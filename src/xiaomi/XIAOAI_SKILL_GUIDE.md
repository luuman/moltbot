# 小爱开放平台技能 - 接收语音命令的完整方案

## 🎯 目标

实现：当你说"小爱同学，打开贾维斯"时，你的代码能收到这个命令并响应。

## 📋 方案概览

### 可行方案对比

| 方案 | 能否收到命令 | 实现难度 | 推荐度 |
|------|-------------|----------|--------|
| 小爱开放平台技能 | ✅ 完整接收 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ |
| 米家虚拟设备 + 轮询 | ⚠️ 只能检测开关 | ⭐ 简单 | ⭐⭐⭐ |
| MIoT API 轮询 | ❌ 无法接收 | - | ❌ |

---

## 方案 1：小爱开放平台技能（推荐）⭐⭐⭐⭐⭐

### 工作流程

```
┌─────────────┐
│  用户说话    │ "小爱同学，让贾维斯查询天气"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  小米云识别  │ query = "让贾维斯查询天气"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 匹配技能     │ 技能名称 = "贾维斯"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 回调你的服务器│ POST http://your-server.com/jarvis
│              │ {
│              │   "query": "查询天气",
│              │   "slots": { "city": "北京" }
│              │ }
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 你的代码处理 │ 调用天气API，生成响应
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 返回响应     │ {
│              │   "reply": "北京今天晴天，25度"
│              │ }
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 小爱播报     │ "北京今天晴天，25度"
└─────────────┘
```

### 实现步骤

#### Step 1: 注册小爱开放平台账号

1. 访问：https://developers.xiaoai.mi.com/
2. 注册账号（个人账号即可，企业认证更快通过审核）
3. 登录控制台

#### Step 2: 创建技能

**2.1 创建新技能**
- 技能名称：`贾维斯`
- 技能类型：`自定义技能`
- 调用方式：`直接调用` 或 `通过短语调用`

**2.2 配置调用短语（示例）**
```
- "小爱同学，打开贾维斯"
- "小爱同学，让贾维斯帮我XXX"
- "小爱同学，问贾维斯XXX"
- "小爱同学，贾维斯XXX"
```

**2.3 配置服务端URL**
```
服务端URL: https://your-domain.com/api/xiaoai/jarvis
请求方法: POST
```

#### Step 3: 实现服务端接口

创建 `src/xiaomi/skill-server.ts`：

```typescript
import express from 'express';
import { XiaomiClient } from './client.js';

const app = express();
app.use(express.json());

// 小爱音箱 DID（用于播报响应）
const SPEAKER_DID = '289833424';

/**
 * 小爱技能回调接口
 *
 * 请求格式：
 * {
 *   "query": "查询天气",           // 用户说的话（去除"让贾维斯"后的部分）
 *   "slots": {                     // 提取的槽位（如果配置了NLU）
 *     "city": "北京"
 *   },
 *   "session": {                   // 会话信息
 *     "new": true,
 *     "sessionId": "xxx"
 *   }
 * }
 */
app.post('/api/xiaoai/jarvis', async (req, res) => {
  console.log('📞 收到小爱技能请求:', req.body);

  const { query, slots, session } = req.body;

  try {
    // 处理命令
    const response = await handleJarvisCommand(query, slots);

    // 返回响应给小爱
    res.json({
      version: '1.0',
      response: {
        shouldEndSession: true,
        outputSpeech: {
          type: 'PlainText',
          text: response
        }
      }
    });

    console.log('✅ 返回响应:', response);
  } catch (error) {
    console.error('❌ 处理失败:', error);
    res.json({
      version: '1.0',
      response: {
        shouldEndSession: true,
        outputSpeech: {
          type: 'PlainText',
          text: '抱歉，贾维斯遇到了一些问题'
        }
      }
    });
  }
});

/**
 * 处理贾维斯命令
 */
async function handleJarvisCommand(query: string, slots: any): Promise<string> {
  console.log(`🤖 贾维斯处理命令: ${query}`);

  // 查询时间
  if (query.includes('时间') || query.includes('几点')) {
    const now = new Date();
    return `现在是${now.getHours()}点${now.getMinutes()}分`;
  }

  // 查询日期
  if (query.includes('日期') || query.includes('几号')) {
    const now = new Date();
    return `今天是${now.getMonth() + 1}月${now.getDate()}日`;
  }

  // 查询天气（示例）
  if (query.includes('天气')) {
    const city = slots?.city || '北京';
    // TODO: 调用天气API
    return `${city}今天晴天，温度25度`;
  }

  // 智能家居控制
  if (query.includes('打开') || query.includes('关闭')) {
    // TODO: 调用智能家居API
    return '好的，已为您执行';
  }

  // 对话（接入ChatGPT/Claude）
  if (query) {
    // TODO: 调用AI API
    return `收到您的指令：${query}`;
  }

  return '抱歉，我不太明白您的意思';
}

// 启动服务器
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`🚀 贾维斯技能服务器已启动: http://localhost:${PORT}`);
});
```

#### Step 4: 部署服务器

**选项 A: 使用内网穿透（开发测试）**
```bash
# 使用 ngrok
ngrok http 3000

# 获得公网URL
# https://abc123.ngrok.io
```

**选项 B: 云服务器部署（正式环境）**
```bash
# 1. 购买云服务器（阿里云/腾讯云/AWS）

# 2. 部署代码
scp -r src user@your-server:/app
ssh user@your-server
cd /app && npm install && npm run build

# 3. 使用 PM2 守护进程
pm2 start dist/xiaomi/skill-server.js --name jarvis

# 4. 配置 Nginx 反向代理（可选）
# 添加 SSL 证书（推荐）
```

**选项 C: Serverless 部署**
```bash
# 使用 Vercel
vercel --prod

# 或使用 Cloudflare Workers
# 或使用 AWS Lambda
```

#### Step 5: 配置小爱技能服务端

在小爱开放平台填写：
```
服务端URL: https://your-domain.com/api/xiaoai/jarvis
```

#### Step 6: 测试技能

在开放平台的测试工具中测试：
```
输入: "查询天气"
预期输出: "北京今天晴天，温度25度"
```

#### Step 7: 提交审核

- 填写技能说明
- 上传技能图标
- 提交审核（通常1-3个工作日）

---

## 方案 2：米家虚拟设备 + 轮询 ⭐⭐⭐

### 原理

在米家App中创建一个虚拟开关设备叫"贾维斯"，通过轮询检测状态变化。

### 实现步骤

#### Step 1: 创建虚拟设备（需要智能设备支持）

**选项 A: 使用智能插座作为"信号灯"**
```typescript
// 监听插座状态
const JARVIS_SWITCH_DID = 'xxx'; // 某个智能插座的DID

setInterval(async () => {
  const state = await httpClient.getProperties([
    { did: JARVIS_SWITCH_DID, siid: 2, piid: 1 } // 开关状态
  ]);

  if (state[0].value === true) {
    console.log('🤖 贾维斯被激活！');

    // 执行你的逻辑
    await handleJarvisActivation();

    // 重置开关
    await httpClient.setProperties([
      { did: JARVIS_SWITCH_DID, siid: 2, piid: 1, value: false }
    ]);
  }
}, 1000); // 每秒检查一次
```

**使用方式**：
```
用户: "小爱同学，打开贾维斯"
小爱: （执行打开智能插座）
你的代码: （检测到插座被打开，触发逻辑）
```

**缺点**：
- ❌ 无法获取用户说的具体内容
- ❌ 只能检测开/关状态
- ❌ 需要占用一个智能设备
- ⚠️ 轮询频率受限

---

## 方案对比总结

| 特性 | 小爱开放平台技能 | 虚拟设备轮询 |
|------|-----------------|-------------|
| 接收完整命令 | ✅ 是 | ❌ 否 |
| 实现复杂度 | ⭐⭐⭐ 中等 | ⭐ 简单 |
| 需要服务器 | ✅ 需要 | ❌ 不需要 |
| 需要审核 | ✅ 需要 | ❌ 不需要 |
| 延迟 | 低（秒级） | 高（轮询间隔） |
| 可扩展性 | ✅ 强 | ❌ 弱 |
| 是否占用设备 | ❌ 否 | ✅ 是 |

---

## 实际使用示例

### 场景 1: 智能对话

**用户**: "小爱同学，让贾维斯查询天气"

**服务器收到**:
```json
{
  "query": "查询天气",
  "slots": { "city": "北京" }
}
```

**你的处理**:
```typescript
const weather = await fetch('https://api.weather.com/...');
return `北京今天${weather.desc}，温度${weather.temp}度`;
```

**小爱播报**: "北京今天晴天，温度25度"

---

### 场景 2: 智能家居控制

**用户**: "小爱同学，让贾维斯打开客厅灯"

**服务器收到**:
```json
{
  "query": "打开客厅灯",
  "slots": { "device": "客厅灯", "action": "打开" }
}
```

**你的处理**:
```typescript
// 控制智能灯
await controlDevice('客厅灯', 'on');
return '好的，已为您打开客厅灯';
```

---

### 场景 3: ChatGPT 对话

**用户**: "小爱同学，问贾维斯什么是量子计算"

**服务器收到**:
```json
{
  "query": "什么是量子计算"
}
```

**你的处理**:
```typescript
const gptResponse = await callChatGPT(query);
return gptResponse; // "量子计算是一种基于量子力学原理的计算方式..."
```

---

## 完整代码示例

### Express 服务器 (完整版)

```typescript
import express from 'express';
import { XiaomiClient } from './client.js';

const app = express();
app.use(express.json());

// 初始化小米客户端
const xiaomiClient = new XiaomiClient();
await xiaomiClient.init();

/**
 * 小爱技能入口
 */
app.post('/api/xiaoai/jarvis', async (req, res) => {
  const { query, slots, session } = req.body;

  console.log('📞 收到请求:', { query, slots });

  try {
    const response = await processCommand(query, slots);

    res.json({
      version: '1.0',
      response: {
        shouldEndSession: true,
        outputSpeech: {
          type: 'PlainText',
          text: response
        }
      }
    });
  } catch (error) {
    console.error('❌ 错误:', error);
    res.json({
      version: '1.0',
      response: {
        shouldEndSession: true,
        outputSpeech: {
          type: 'PlainText',
          text: '抱歉，出现了一些问题'
        }
      }
    });
  }
});

/**
 * 命令处理器
 */
async function processCommand(query: string, slots: any): Promise<string> {
  // 时间查询
  if (/时间|几点/.test(query)) {
    const now = new Date();
    return `现在是${now.getHours()}点${now.getMinutes()}分`;
  }

  // 天气查询
  if (/天气/.test(query)) {
    const city = slots?.city || '北京';
    const weather = await getWeather(city);
    return `${city}今天${weather.desc}，温度${weather.temp}度`;
  }

  // 智能家居
  if (/打开|关闭/.test(query)) {
    const device = slots?.device || extractDevice(query);
    const action = query.includes('打开') ? 'on' : 'off';
    await controlDevice(device, action);
    return `好的，已为您${action === 'on' ? '打开' : '关闭'}${device}`;
  }

  // ChatGPT 对话
  return await askChatGPT(query);
}

app.listen(3000, () => {
  console.log('🚀 贾维斯服务器启动成功');
});
```

---

## FAQ

### Q1: 必须要有服务器吗？
**A**: 是的，小爱开放平台需要一个公网可访问的服务器来接收回调。

**解决方案**:
- 开发测试：使用 ngrok 内网穿透
- 正式环境：云服务器（阿里云/腾讯云）或 Serverless（Vercel/Cloudflare Workers）

### Q2: 审核需要多久？
**A**: 通常1-3个工作日。个人技能审核可能更快。

### Q3: 可以不审核直接用吗？
**A**: 可以，使用"测试版本"功能，但只有你自己的账号能用。

### Q4: 服务器挂了怎么办？
**A**: 小爱会返回默认错误提示。建议：
- 使用云服务器的自动重启
- 使用 PM2 守护进程
- 配置监控告警

### Q5: 能接收到完整的语音吗？
**A**: 不能，只能接收到**识别后的文字**。

---

## 总结

**推荐方案**: 小爱开放平台技能

**优势**:
- ✅ 能接收完整的语音识别文字
- ✅ 可以自定义触发短语
- ✅ 可扩展性强（接入ChatGPT、智能家居等）

**劣势**:
- ❌ 需要服务器
- ❌ 需要审核（测试版不需要）

**适合场景**:
- 智能对话助手
- 智能家居语音控制
- 定制化语音服务

**下一步**:
1. 注册小爱开放平台账号
2. 创建"贾维斯"技能
3. 部署服务器（可以先用 ngrok 测试）
4. 测试技能
5. 提交审核

需要我帮您实现服务器代码吗？🚀
